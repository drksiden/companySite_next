import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Административная панель',
  robots: {
    index: false,
    follow: false,
    noarchive: true,
    nosnippet: true,
  },
};

