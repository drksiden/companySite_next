# Следующие шаги после получения данных из Grafana Cloud

## ✅ У вас должно быть 4 значения:

1. **Instance ID** (например: `123456`)
2. **API Token** (начинается с `glc_...`)
3. **Loki URL** (например: `https://logs-prod-us.grafana.net/loki/api/v1/push`)
4. **Prometheus URL** (например: `https://prometheus-prod-us.grafana.net/api/prom/push`)

## 🚀 Шаг 1: Установка Grafana Alloy на сервере

Grafana Alloy будет собирать метрики системы и логи приложения и отправлять их в Grafana Cloud.

### На вашем сервере выполните:

```bash
# Перейдите в директорию проекта
cd /path/to/companySite_next

# Запустите скрипт установки
sudo ./scripts/setup-grafana-cloud.sh
```

Скрипт запросит:
- Instance ID
- API Token
- Loki URL
- Prometheus URL

Введите все 4 значения по очереди.

### Что делает скрипт:

- ✅ Устанавливает Grafana Alloy
- ✅ Устанавливает Node Exporter (метрики системы)
- ✅ Настраивает сбор логов из Docker
- ✅ Настраивает отправку метрик и логов в Grafana Cloud
- ✅ Запускает все сервисы

### Проверка установки:

```bash
# Проверьте статус Alloy
sudo systemctl status alloy

# Проверьте статус Node Exporter
sudo systemctl status node_exporter

# Или используйте готовый скрипт проверки
./scripts/check-grafana-cloud.sh
```

## 🔧 Шаг 2: Настройка приложения

Теперь нужно настроить ваше приложение, чтобы оно отправляло логи в Grafana Cloud.

### Добавьте в `.env` на сервере:

```env
# Grafana Cloud Loki
GRAFANA_LOKI_URL=https://logs-prod-us.grafana.net/loki/api/v1/push
GRAFANA_LOKI_LABELS={"app":"company-site-next","environment":"production"}
GRAFANA_LOKI_BASIC_AUTH=YOUR_INSTANCE_ID:YOUR_API_TOKEN

# Уровень логирования
LOG_LEVEL=info
```

**Важно:**
- Замените `YOUR_INSTANCE_ID` на ваш Instance ID
- Замените `YOUR_API_TOKEN` на ваш API Token
- Замените URL на ваш Loki URL (если отличается)

### Формат GRAFANA_LOKI_BASIC_AUTH:

```
INSTANCE_ID:API_TOKEN
```

Например:
```
123456:glc_eyJvIjoiMTIzNDU2IiwibiI6ImFsbG95LXRva2VuIiwiaSI6IjEyMzQ1NiIsImsiOiJhYmNkZWZnaGlqa2xtbm9wcXJzdHV2d3h5eiJ9
```

### Перезапустите приложение:

```bash
# Если используете Docker Compose
docker-compose restart asia-ntb

# Или если используете другой способ запуска
# перезапустите приложение соответствующим образом
```

## ✅ Шаг 3: Проверка работы

### 3.1. Проверьте отправку логов

```bash
# Вызовите тестовый endpoint (если есть)
curl http://localhost:22742/api/test-logging

# Или просто откройте сайт и выполните какое-то действие
```

### 3.2. Проверьте в Grafana Cloud

1. Откройте ваш Grafana Cloud dashboard
2. Перейдите в **Explore** → **Loki**
3. Введите запрос:
   ```
   {app="company-site-next"}
   ```
4. Нажмите **Run query**
5. Должны появиться логи!

### 3.3. Проверьте метрики

1. В Grafana Cloud перейдите в **Explore** → **Prometheus**
2. Введите запрос:
   ```
   up
   ```
3. Нажмите **Run query**
4. Должны появиться метрики!

## 📊 Шаг 4: Настройка Synthetic Monitoring (проверка доступности сайта)

Synthetic Monitoring будет проверять ваш сайт каждую минуту из разных точек мира.

### В Grafana Cloud:

1. Перейдите в **Synthetic Monitoring** (в левом меню)
2. Нажмите **Add new check** или **Create check**
3. Выберите **HTTP** check
4. Заполните:
   - **Name**: `Company Site Availability`
   - **URL**: `https://your-domain.com` (ваш реальный домен)
   - **Frequency**: `1 minute`
   - **Regions**: выберите несколько регионов (US, EU, APAC)
   - **Timeout**: `10 seconds`
5. Нажмите **Create** или **Save**

Теперь Grafana будет проверять ваш сайт каждую минуту!

## 🔔 Шаг 5: Настройка уведомлений (опционально)

### Уведомления в Telegram:

1. Создайте бота через @BotFather в Telegram
2. Получите токен бота
3. Узнайте свой Chat ID (напишите @userinfobot)
4. В Grafana Cloud:
   - **Alerting** → **Notification channels**
   - **Add channel** → **Telegram**
   - Введите токен бота и Chat ID
   - Сохраните
5. Создайте правило алерта:
   - **Alerting** → **Alert rules** → **New alert rule**
   - Условие: `probe_success{job="http"} == 0`
   - Уведомление: выберите созданный Telegram канал

## 📈 Шаг 6: Импорт готовых дашбордов

В Grafana Cloud есть библиотека готовых дашбордов:

1. В Grafana перейдите в **Dashboards** → **Import**
2. Импортируйте:
   - **Node Exporter Full** (ID: `1860`) - метрики сервера
   - **Docker** (ID: `179`) - метрики Docker
   - **Loki Logs** (ID: `13639`) - просмотр логов

### Как импортировать:

1. Нажмите **Import**
2. Введите ID дашборда (например: `1860`)
3. Нажмите **Load**
4. Выберите источник данных (Prometheus или Loki)
5. Нажмите **Import**

## 🎯 Полезные запросы в Grafana

### Логи ошибок:
```
{app="company-site-next", level="error"}
```

### Метрики CPU:
```
100 - (avg by (instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)
```

### Метрики памяти:
```
node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes
```

### Доступность сайта:
```
probe_success{job="http"}
```

## 🆘 Troubleshooting

### Логи не появляются в Grafana

1. Проверьте переменные окружения:
   ```bash
   docker exec asia-ntb env | grep GRAFANA
   ```

2. Проверьте логи приложения:
   ```bash
   docker logs asia-ntb | grep -i loki
   ```

3. Проверьте Alloy:
   ```bash
   sudo journalctl -u alloy -n 50
   ```

### Метрики не появляются

1. Проверьте Node Exporter:
   ```bash
   curl http://localhost:9100/metrics
   ```

2. Проверьте Alloy:
   ```bash
   sudo systemctl status alloy
   sudo journalctl -u alloy -f
   ```

### Alloy не запускается

1. Проверьте конфигурацию:
   ```bash
   sudo /usr/local/bin/alloy run --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy --dry-run
   ```

2. Проверьте логи:
   ```bash
   sudo journalctl -u alloy -n 100
   ```

## ✅ Чеклист

- [ ] Grafana Alloy установлен и запущен
- [ ] Node Exporter установлен и запущен
- [ ] Переменные окружения добавлены в `.env`
- [ ] Приложение перезапущено
- [ ] Логи появляются в Grafana Cloud
- [ ] Метрики появляются в Grafana Cloud
- [ ] Synthetic Monitoring настроен
- [ ] Уведомления настроены (опционально)
- [ ] Готовые дашборды импортированы

## 🎉 Готово!

Теперь у вас настроен полный мониторинг:
- ✅ Метрики сервера (CPU, память, диск)
- ✅ Логи приложения
- ✅ Проверка доступности сайта
- ✅ Уведомления при проблемах

## 📚 Дополнительная документация

- Полная настройка: `docs/GRAFANA-CLOUD-SETUP.md`
- Быстрый старт: `README-GRAFANA-CLOUD.md`
- Логирование ошибок: `docs/ERROR-LOGGING.md`

