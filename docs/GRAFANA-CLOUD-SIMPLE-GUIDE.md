# Простая инструкция: Как найти Stack в Grafana Cloud

## 🎯 Если вы только зарегистрировались

### Шаг 1: Откройте портал Grafana Cloud

1. Откройте https://grafana.com
2. Войдите в аккаунт (Sign in)

### Шаг 2: Найдите раздел "Grafana Cloud"

После входа вы увидите главную страницу. Ищите:

- **В левом меню**: раздел "Grafana Cloud" → там будет "Add Stack" или список стеков
- **В центре страницы**: кнопка "Create a stack" или "Add Stack"
- **В верхнем меню**: может быть выпадающий список со стеками

### Шаг 3: Создайте стек (если его нет)

1. Нажмите **"Add Stack"** или **"Create Stack"**
2. Введите имя: `companysitemonitoring` (только маленькие буквы и цифры, без пробелов!)
3. Выберите регион: US, EU или APAC
4. Нажмите **"Create"**

### Шаг 4: Откройте ваш стек

После создания:
- Нажмите на название стека
- Или перейдите по ссылке: `https://companysitemonitoring.grafana.net`

## 🔍 Альтернативный способ: Через URL

Если вы уже где-то видели ссылку на Grafana:

1. Проверьте URL в браузере
2. Если там что-то вроде `https://123456.grafana.net` или `https://yourname.grafana.net`
3. Это и есть ваш стек!

## 📋 Что делать дальше (после того как нашли стек)

### 1. Получите Instance ID

**Способ A: Из URL**
- Если URL: `https://123456.grafana.net` → Instance ID = `123456`
- Если URL: `https://companysitemonitoring.grafana.net` → используйте название

**Способ B: Из настроек**
1. В Grafana нажмите на **шестеренку** (Configuration) в левом меню
2. Выберите **Preferences**
3. Там будет **Organization ID** или **Stack ID**

### 2. Создайте API Token

1. В Grafana нажмите на **шестеренку** (Configuration)
2. Выберите **API Keys**
3. Нажмите **New API Key**
4. Имя: `monitoring-token`
5. Role: `Admin`
6. Нажмите **Add**
7. **Скопируйте токен сразу!** (показывается один раз)

### 3. Получите Loki URL

**Способ A: Через Connections**
1. В левом меню найдите **Connections** (или **Data sources**)
2. Нажмите **Add connection** или **Add data source**
3. Выберите **Loki**
4. В настройках найдите **Push URL** или **HTTP URL**
5. Скопируйте URL

**Способ B: Стандартный URL**
- Если регион US: `https://logs-prod-us.grafana.net/loki/api/v1/push`
- Если регион EU: `https://logs-prod-eu.grafana.net/loki/api/v1/push`
- Если регион APAC: `https://logs-prod-ap.grafana.net/loki/api/v1/push`

### 4. Получите Prometheus URL

**Способ A: Через Connections**
1. Connections → Add connection → Prometheus
2. Найдите **Remote Write Endpoint**
3. Скопируйте URL

**Способ B: Стандартный URL**
- US: `https://prometheus-prod-us.grafana.net/api/prom/push`
- EU: `https://prometheus-prod-eu.grafana.net/api/prom/push`
- APAC: `https://prometheus-prod-ap.grafana.net/api/prom/push`

## 🆘 Если все еще не можете найти

### Вариант 1: Сделайте скриншот

Сделайте скриншот:
- Главной страницы после входа
- Левого меню
- Верхнего меню

И я помогу найти нужные разделы!

### Вариант 2: Используйте локальный стек

Если Grafana Cloud не подходит, используйте локальный Docker Compose:

```bash
# Используйте локальный стек
docker-compose -f docker-compose.production.yml up -d
```

См. инструкцию: `docs/QUICK-START-PRODUCTION.md`

### Вариант 3: Проверьте email

После регистрации должен прийти email со ссылкой на ваш стек. Проверьте почту!

## ✅ Чеклист

После того как нашли стек, у вас должно быть:

- [ ] Instance ID (число или название)
- [ ] API Token (начинается с `glc_`)
- [ ] Loki URL (начинается с `https://logs-prod-`)
- [ ] Prometheus URL (начинается с `https://prometheus-prod-`)

## 🚀 Следующий шаг

После получения всех данных:

```bash
sudo ./scripts/setup-grafana-cloud.sh
```

Скрипт запросит эти 4 значения.

