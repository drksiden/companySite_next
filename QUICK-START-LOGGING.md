# 🚀 Быстрый старт: Локальное логирование

## 1. Запустите стек логирования

```bash
pnpm run logging:start
```

Или вручную:
```bash
docker-compose -f docker-compose.logging.yml up -d
```

## 2. Настройте переменные окружения

Создайте `.env.local` или добавьте в `.env`:

```env
LOG_LEVEL=debug
GRAFANA_LOKI_URL=http://localhost:3100
GRAFANA_LOKI_LABELS={"app":"company-site-next","environment":"development"}
```

## 3. Запустите приложение

```bash
pnpm dev
```

## 4. Протестируйте логирование

Откройте в браузере:
```
http://localhost:3000/api/test-logging
```

Это отправит тестовые логи всех уровней.

## 5. Просмотрите логи в Grafana

1. Откройте http://localhost:3001
2. Войдите: `admin` / `admin`
3. Перейдите в **Explore** (иконка компаса)
4. Выберите **Loki**
5. Введите: `{app="company-site-next"}`

## Готово! 🎉

Теперь все логи вашего приложения будут отправляться в Grafana Loki.

---

**Остановка стека:**
```bash
pnpm run logging:stop
```

**Просмотр логов контейнеров:**
```bash
pnpm run logging:logs
```
