# 🚀 Быстрый старт: Логирование с Grafana Loki

## Шаг 1: Запуск стека логирования

```bash
# Вариант 1: Использовать скрипт
pnpm run logging:start

# Вариант 2: Использовать docker-compose напрямую
docker-compose -f docker-compose.logging.yml up -d
```

Это запустит:
- **Grafana** на http://localhost:3001 (admin/admin)
- **Loki** на http://localhost:3100
- **Promtail** для сбора логов

## Шаг 2: Настройка переменных окружения

Создайте файл `.env.local` (или добавьте в существующий `.env`):

```env
LOG_LEVEL=debug
GRAFANA_LOKI_URL=http://localhost:3100
GRAFANA_LOKI_LABELS={"app":"company-site-next","environment":"development"}
```

Можно скопировать из примера:
```bash
cp .env.local.example .env.local
```

## Шаг 3: Запуск приложения

```bash
pnpm dev
```

## Шаг 4: Просмотр логов в Grafana

1. Откройте http://localhost:3001
2. Войдите: `admin` / `admin`
3. Перейдите в **Explore** (иконка компаса слева)
4. Выберите источник данных **Loki**
5. Введите запрос: `{app="company-site-next"}`

### Полезные запросы:

- Все логи: `{app="company-site-next"}`
- Только ошибки: `{app="company-site-next", level="error"}`
- HTTP запросы: `{app="company-site-next", level="http"}`
- По времени: `{app="company-site-next"} |~ "2024"`

## Шаг 5: Использование дашборда

Дашборд "Company Site - Application Logs" должен появиться автоматически.

Если нет:
1. Перейдите в **Dashboards** → **Browse**
2. Или импортируйте вручную из `grafana/provisioning/dashboards/company-site-dashboard.json`

## Остановка стека

```bash
# Вариант 1: Использовать скрипт
pnpm run logging:stop

# Вариант 2: Использовать docker-compose напрямую
docker-compose -f docker-compose.logging.yml down
```

## Просмотр логов контейнеров

```bash
pnpm run logging:logs
```

## Устранение проблем

### Loki не запускается
```bash
# Проверьте логи
docker-compose -f docker-compose.logging.yml logs loki

# Проверьте, не занят ли порт 3100
lsof -i :3100
```

### Grafana не подключается к Loki
1. Проверьте, что оба контейнера в одной сети: `docker network ls`
2. Проверьте настройки в Grafana: Configuration → Data Sources → Loki
3. URL должен быть: `http://loki:3100`

### Логи не появляются в Grafana
1. Убедитесь, что переменные окружения настроены правильно
2. Проверьте логи приложения: `pnpm dev` должен показывать логи в консоли
3. Проверьте подключение к Loki: `curl http://localhost:3100/ready`

## Дополнительная информация

Подробная документация: [docs/LOGGING.md](./docs/LOGGING.md)

